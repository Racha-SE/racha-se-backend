export const categoriesService = {
  list(): Promise<null> {
    return Promise.resolve(null);
  },

  getById(): Promise<null> {
    return Promise.resolve(null);
  },

  create(): Promise<null> {
    return Promise.resolve(null);
  },

  update(): Promise<null> {
    return Promise.resolve(null);
  },

  remove(): Promise<null> {
    return Promise.resolve(null);
  },
};
